# ===================== 求职匹配 AI 系统 依赖清单 =====================
# Python >= 3.10，全本地私有化部署，无任何云端大模型依赖
# 安装：pip install -r requirements.txt

# ---- Web 框架 ----
# fastmcp-slim[client,server] 拉 starlette>=1.x;fastapi 0.121 锁 <0.50 → 升到 0.141+ 解锁
fastapi>=0.130.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ---- 配置 ----
# fastmcp-slim 要求 python-dotenv>=1.1
python-dotenv>=1.1.0

# ---- 应用框架 ----
langchain==0.2.5
langchain-text-splitters==0.2.0

# ---- 向量数据库 ----
chromadb==0.5.5

# ---- 关系型数据库 ----
pymysql==1.1.0
DBUtils==3.0.3
cryptography==42.0.8

# ---- 文档解析 ----
PyMuPDF==1.24.7
python-docx==1.1.0

# ---- 检索 ----
rank-bm25==0.2.2
jieba==0.42.1

# ---- 数据处理与可视化 ----
pandas==2.2.2
matplotlib==3.9.0

# ---- HTTP 客户端 ----
requests==2.32.3

# ---- 前端 ----
streamlit==1.36.0

# ---- MCP 协议工具(2026-08 接入) ----
fastmcp==3.4.7
