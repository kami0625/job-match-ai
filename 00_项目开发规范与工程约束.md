# 项目开发规范与工程约束说明书
## 一、总则
1.  本规范适用于本项目所有模块代码（RAG匹配模块、简历评分Agent模块），所有代码生成、编写必须严格遵守。
2.  项目核心约束：全本地私有化部署，**禁止任何第三方云端大模型API调用**，所有大模型能力统一通过本地 Ollama 实现。
3.  架构原则：前后端分离，后端分层解耦，通用能力下沉复用，禁止业务模块重复实现基础功能。

## 二、工程目录结构规范
### 固定目录结构，禁止随意新增根级目录
job_match_ai_project/
├── docs/ # 所有项目文档存放处
│ ├── 00_项目开发规范与工程约束.md
│ ├── 01_RAG 求职匹配系统_技术规格说明书.md
│ └── 02_简历评分优化助手 Agent_技术规格说明书.md
├── app/ # FastAPI 后端主目录
│ ├── main.py # 项目启动入口，统一挂载所有路由
│ ├── config.py # 全局配置读取，统一管理所有参数
│ ├── utils/ # 通用工具层，所有模块共享
│ │ ├── ollama_client.py # Ollama 统一调用封装
│ │ ├── doc_parser.py # PDF/Word 文档解析工具
│ │ └── common_tools.py # 通用工具函数
│ ├── dao/ # 数据访问层，所有数据库操作
│ │ ├── mysql_db.py # MySQL 连接与基础操作
│ │ └── chroma_db.py # Chroma 向量库连接与基础操作
│ └── modules/ # 业务模块目录，各业务独立文件夹
│ ├── rag_module/ # RAG 求职匹配业务模块
│ │ ├── api.py # 模块接口定义
│ │ └── service.py # 模块业务逻辑
│ └── agent_module/ # 简历评分 Agent 业务模块
│ ├── api.py # 模块接口定义
│ ├── service.py # 模块业务逻辑
│ └── tools.py # Agent 自定义工具集
├── frontend/ # Streamlit 前端目录
│ ├── Home.py # 首页
│ └── pages/ # 多页面，按业务拆分
│ ├── 1_岗位智能问答.py
│ └── 2_简历评分优化.py
├── data/ # 本地数据存储
│ ├── chroma_db/ # Chroma 向量库持久化文件
│ └── upload_files/ # 用户上传的文档、简历临时存储
├── .env.example # 环境变量示例文件
├── requirements.txt # 全项目统一依赖清单
└── README.md # 项目说明文档

### 目录职责约束
1.  `api.py`：仅负责参数接收、校验、返回结果封装，不写业务逻辑。
2.  `service.py`：仅负责业务逻辑编排，不直接操作数据库、不直接调用大模型。
3.  `dao/`、`utils/`：仅负责底层能力封装，不包含业务逻辑。
4.  业务模块之间禁止直接跨模块 import 内部函数，联动必须通过对方的 service 层接口调用。

## 三、代码命名规范（严格遵循 PEP8）
1.  **文件/目录命名**：全小写，下划线分隔，例如 `rag_module.py`
2.  **变量/函数命名**：全小写，下划线分隔，见名知意，例如 `calculate_match_score`
3.  **类命名**：大驼峰，例如 `OllamaClient`
4.  **常量命名**：全大写，下划线分隔，在 config.py 中统一定义，例如 `OLLAMA_BASE_URL`
5.  **接口路径命名**：全小写，斜杠分隔，业务前缀统一，例如 `/api/rag/chat`、`/api/agent/score`
6.  禁止使用拼音、无意义单字符命名，禁止中英文混合命名。

## 四、配置与依赖规范
1.  **环境变量管理**：敏感配置（数据库账号、端口等）统一通过 `.env` 文件管理，使用 `python-dotenv` 读取，禁止硬编码在代码中。
2.  **配置统一入口**：所有配置统一在 `app/config.py` 中读取并暴露，业务代码只能从 config.py 引入配置，禁止直接读取环境变量。
3.  **依赖管理**：所有第三方库统一写入根目录 `requirements.txt`，标注稳定版本号，禁止业务模块单独引入未登记的依赖。
4.  核心依赖固定版本范围：
    - Python >= 3.10
    - fastapi >= 0.110.0
    - langchain >= 0.2.0
    - chromadb >= 0.5.0
    - pymysql >= 1.1.0
    - pandas >= 2.0.0
    - streamlit >= 1.32.0

## 五、接口返回与异常处理规范
### 1. 统一响应格式
所有接口必须返回统一 JSON 结构，禁止各接口自定义格式：
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
code：状态码，200 成功，400 参数错误，500 服务内部错误
message：错误描述或成功提示
data：业务返回数据

###2. 异常处理规范
所有接口必须添加全局异常捕获，禁止服务直接崩溃返回堆栈信息。
参数校验失败返回 400 状态码，明确标注错误字段与原因。
大模型调用、数据库操作必须添加超时与异常捕获，失败返回明确错误信息，不得透传底层报错。
禁止在业务代码中裸露 print 调试，统一使用日志模块输出。

##六、大模型调用统一规范
唯一入口：所有大模型、嵌入模型调用必须通过 app/utils/ollama_client.py 封装类实现，业务代码禁止直接使用 requests 调用 11434 端口。
统一封装能力：
单轮对话生成
多轮对话生成
文本嵌入向量生成
流式输出支持
模型名称、接口地址、超时时间、温度参数全部从 config.py 读取，禁止硬编码在业务逻辑中。
调用失败必须有重试机制，默认重试 2 次，重试失败抛出统一异常。

##七、日志规范
统一使用 Python 标准 logging 模块，在 utils 中封装统一日志工具。
日志级别：DEBUG / INFO / WARNING / ERROR，业务流程关键节点输出 INFO 日志，异常输出 ERROR 并附带堆栈。
日志格式固定：[时间] [级别] [模块名] 日志内容
禁止输出用户上传的完整简历、文档等敏感数据到日志中。

##八、开发指令约束
每次生成代码前，必须优先参考本文档规范，再对应业务模块的规格说明书。
生成代码时必须遵守以下硬性要求：
严格遵循分层架构，不得跨层调用、不得职责混乱。
全程仅使用本地 Ollama 模型，禁止出现任何 OpenAI、DeepSeek、通义千问等云端 API 相关代码。
复用 utils、dao 层已有工具，禁止重复实现相同功能。
所有接口遵循统一响应格式，必须包含异常处理。
关键函数必须添加功能说明、入参、出参注释。