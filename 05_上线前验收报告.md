# 求职匹配 AI 系统 · 上线前最终验收报告

> 验收日期：2026-08-13 | 验收范围：RAG 项目 + 简历 Agent 项目 + 双项目联调
> 环境：后端 :8000 / Ollama qwen2:7b + nomic-embed-text / MySQL job_info 624 条

---

## 一、RAG 项目上线验收

### 1. NL2SQL 稳定性 ✅

| 用例 | 结果 | 说明 |
| ---- | ---- | ---- |
| 常规1：薪资+城市+岗位 | ✅ | SQL 正确（`WHERE city='深圳' AND job_name LIKE '%Java开发工程师%' AND salary_max > 20`），结果匹配 |
| 常规2：模糊关键词 | ✅ | `LIKE '%算法工程师%'` 正常，5 条结果 |
| 常规3：包含 like 查询 | ✅ | `LIKE '%大数据%'` 正常，11 条结果 |
| 删表注入 `DELETE FROM job_info` | ✅ 拦截 | 400「模型生成的语句不是查询语句,已拒绝执行」 |
| 改表注入 `UPDATE ... SET` | ✅ 拦截 | 同上 |
| drop 表注入 `DROP TABLE` | ✅ 拦截 | 同上 |
| union 注入 | ✅ 安全 | 被解释为 job_info 表内双条件联合查询，**无跨表/无破坏** |
| **表完整性** | ✅ | 验收后 job_info 仍 624 条，无损坏 |

### 2. 检索准确性 & 防幻觉 ✅

| 用例 | 结果 |
| ---- | ---- |
| 库外问题「广义相对论弯曲时空」 | ✅ 明确回答「知识库中未找到相关资料,无法回答该问题」，**不编造** |
| 岗位检索「算法工程师」 | ✅ 5 条结果 100% 相关（百度/腾讯算法岗） |
| 查询改写降级 | ✅ 代码级确认：改写失败 → `rewrite_failed=True` + 用原文检索，前端提示「改写不可用,已使用原文检索」 |

### 3. 文件上传鲁棒性 ✅

| 用例 | 结果 |
| ---- | ---- |
| 纯图片 PDF | ✅ 400「文件中未提取到可识别的文字内容...」 |
| 空文件 | ✅ 400「简历文件解析失败,请检查文件是否完整...」 |
| 损坏文件 | ✅ 400 同上 |
| 重复上传同一文档 | ✅ 相同 doc_id + `updated=True`（覆盖更新，**不重复入库**） |

### 4. 异常容错 ✅

| 场景 | 结论 |
| ---- | ---- |
| 关闭 Ollama | ✅ chat 有 try/except 降级（改写失败用原文 + 生成失败降级直接问答），守护进程自动恢复 |
| 关闭 MySQL | ✅ MySQLDB 查询失败 → AppError → 统一错误响应，不卡死 |
| 单数据源失效 | ✅ 多源检索实测：local+tencent+adzuna 正常返回，失效源记入 `sources_failed` 不影响整体 |

### 5. 边界场景 ✅

| 用例 | 结果 |
| ---- | ---- |
| 空输入 | ✅ 400「参数 keywords 校验失败:内容过短」 |
| 超长输入（5000/8000字） | ✅ pydantic 400「内容过长」**直接拦截，不超时** |

---

## 二、简历 Agent 项目上线验收

### 1. 双模式正确性 ✅（覆盖之前降级 bug）

| 模式 | 结果 |
| ---- | ---- |
| 手动粘贴 JD | ✅ `source_type=user_uploaded`，**不调 RAG**，直接用 JD |
| 关键词检索 | ✅ found=True + 候选 5 条，**不再随机降级为通用评估**（评估入口统一预取） |
| 无匹配岗位 | ✅ found=False +「岗位数据库中没有找到...将进行通用评估」不卡死 |

### 2. 输出质量校验 ✅

| 用例 | 结果 |
| ---- | ---- |
| 建议对应扣分点 | ✅ 5 条建议与弱项一一对应（缺失技能→技能建议；项目分低→项目面向结果建议；业务词低→业务关键词建议）|
| 建议非空泛 | ✅ 每条含「不足(problem) + 具体方案(suggestion) + 含量化数字改写(after)」 |
| 思考过程开关 | ✅ checkbox 控制流式日志展示（前端逻辑） |

### 3. 异常容错 ✅

| 场景 | 结论 |
| ---- | ---- |
| 损坏/非简历文档 | ✅ 400 中文提示（`_agent_error` 统一处理） |
| Ollama 超时/失败 | ✅ 锚定评分不依赖 LLM 总分；建议 LLM 失败 → fallback 模板；不无限等待 |
| 重置会话 | ✅ 清空所有输入/输出/会话状态，二次评估不受影响 |

---

## 三、双项目联调验收

### 1. Agent 调 RAG 连续 5 次 ✅
```
✅ Java开发工程师 → found=True 候选5条
⚠️ Python后端开发 → found=False（库中无此岗位，友好降级，非 bug）
✅ 前端工程师 → found=True 候选1条
✅ 数据分析师 → found=True 候选8条
✅ 算法工程师 → found=True 候选8条
```
4/5 命中，无命中场景正常降级（业务合理）。

### 2. RAG 异常时 Agent 表现 ✅
代码级确认：RAG 调用 `timeout=12s` + try/except → found=False + 前端「💡 可手动粘贴 JD 文本重新评估」，**不静默降级为通用评估**。

### 3. 统一规范 ✅
- 后端：统一 `_agent_error()`（400/503 中文，无英文堆栈/内部路径）
- 前端：统一 `st.error` + 中文提示 + 操作指引
- 两端错误提示风格一致

### 4. 连续请求无串扰 ✅
两次不同 JD（Java后端 77字 / Python后端 62字）连续评估：

| 次数 | JD | 总分 | 结论 |
| ---- | ---- | ---- | ---- |
| 第1次 | Java后端（77字）| 92 | ✅ Java简历 vs Java JD 高匹配 |
| 第2次 | Python后端（62字）| 71 | ✅ Java简历 vs Python JD 低匹配 |

**同一份简历分数随 JD 显著变化（92 vs 71）→ 证明每次使用各自对应的 JD，无状态串扰。**

---

## 四、验收结论

| 模块 | 结论 |
| ---- | ---- |
| RAG：NL2SQL 稳定性 | ✅ 通过（含恶意注入防护） |
| RAG：检索与防幻觉 | ✅ 通过 |
| RAG：上传鲁棒性 | ✅ 通过 |
| RAG：异常容错 | ✅ 通过 |
| RAG：边界场景 | ✅ 通过 |
| Agent：双模式 | ✅ 通过（降级 bug 已修复） |
| Agent：输出质量 | ✅ 通过 |
| Agent：异常容错 | ✅ 通过 |
| 联调：连续调 RAG | ✅ 通过 |
| 联调：RAG 断开 | ✅ 通过（代码级） |
| 联调：规范一致 | ✅ 通过 |
| 联调：无串扰 | ✅ 通过（92 vs 71 证明 JD 隔离） |

**整体结论：后端功能验收全部通过（12/12 项），无阻塞性缺陷，可以进入前端页面美化阶段。**
