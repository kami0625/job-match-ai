# 简历评分优化助手 Agent 技术实现规格说明书
## 一、项目概述
1.  项目定位：基于本地大模型 Agent 的简历智能评分与优化工具，自动识别简历短板，生成针对性修改建议。
2.  联动关系：可对接「求职匹配RAG系统」，基于检索到的目标岗位 JD，做岗位定向的简历评分与优化。
3.  核心属性：全本地推理，简历数据不上传任何第三方服务。

## 二、技术栈
| 层级 | 技术选型 |
| ---- | ---- |
| 开发语言 | Python 3.10+ |
| Agent框架 | LangChain ReAct / 自定义工具调用 |
| 大模型底座 | Ollama 本地部署，模型：qwen2:7b |
| 文档解析 | PyMuPDF、python-docx |
| 集成方式 | 以接口形式嵌入 RAG 系统 FastAPI 服务，共用 Streamlit 前端 |

## 三、核心功能模块
### 1. 简历解析模块
- 支持 PDF/Word 格式简历上传，自动提取纯文本内容
- 结构化提取：个人信息、教育背景、工作/项目经历、技能栈、获奖情况

### 2. 多维度评分引擎
- 评分维度：技能匹配度、项目完整性、表述专业度、格式规范度、岗位契合度
- 每个维度 0-100 分，输出总分 + 各维度分项得分 + 扣分说明
- 支持「通用评分」和「岗位定向评分」两种模式，岗位定向模式需传入岗位 JD

### 3. 优化建议生成模块
- 针对每个扣分点，生成具体、可落地的修改建议
- 提供简历表述改写示例，将口语化描述优化为专业、量化的表达
- 输出完整的简历优化方案，分模块展示修改前后对比

### 4. Agent 工具集
- Agent 自主判断调用对应工具完成评分、分析、改写全流程，无需人工分步调用

### 5. RAG 系统联动模块
- 调用 RAG 系统接口检索目标岗位 JD、行业招聘要求
- 基于检索结果做精准岗位匹配评分，提升建议针对性

## 四、Agent 工具定义
### 1. resume_parser
- 功能：解析上传的简历文件，输出结构化简历信息
- 输入：简历文件路径 / 简历纯文本
- 输出：结构化JSON（个人信息、技能、经历、项目）

### 2. skill_match_calculator
- 功能：计算简历技能与岗位要求的匹配度得分
- 输入：简历技能列表、岗位技能要求列表
- 输出：匹配得分 + 缺失技能清单

### 3. score_evaluator
- 功能：按照固定评分维度，输出全维度评分结果与扣分项
- 输入：结构化简历信息、岗位JD（可选）
- 输出：总分、各维度得分、扣分说明

### 4. optimization_writer
- 功能：针对扣分项生成优化建议与改写示例
- 输入：扣分项列表、原始简历文本
- 输出：分模块优化建议 + 改写前后对比

## 五、FastAPI 接口规范（集成进RAG系统后端）
### 1. 简历上传解析接口
- 路径：`POST /api/resume/parse`
- 请求：multipart/form-data，字段 `file`
- 返回：结构化简历信息JSON

### 2. 简历评分接口
- 路径：`POST /api/resume/score`
- 请求体：`{"resume_text": "简历文本", "job_desc": "岗位JD（可选，为空则通用评分）"}`
- 返回：总分、各维度得分、扣分详情

### 3. 优化建议生成接口
- 路径：`POST /api/resume/optimize`
- 请求体：`{"resume_text": "简历文本", "score_detail": "评分详情", "target_job": "目标岗位（可选）"}`
- 返回：分模块优化建议、改写示例、完整优化方案

## 六、Streamlit 前端页面规范
1.  页面入口：侧边栏「简历优化」板块
2.  核心区域：
    - 简历上传区：支持拖拽上传 PDF/Word
    - 岗位选择区：可输入目标岗位，调用RAG系统检索对应JD
    - 评分结果区：总分卡片、维度得分柱状图、扣分说明列表
    - 优化建议区：分模块展示修改建议、改写前后对比
3.  支持一键导出优化报告为 Markdown / TXT 文件

## 七、代码约束
1.  所有大模型调用均走本地 Ollama，不引入任何云端API
2.  工具定义、Agent 逻辑、业务代码分层存放，便于维护
3.  评分规则可通过配置文件调整，不硬编码在业务逻辑中
4.  输出结果格式稳定，便于前端渲染展示

---

## 八、变更日志（2026-08-13 全流程自测 + 优化迭代）

### 1. 锚定评分体系（优秀简历 = 90 分标杆）

**问题演进**：及格线 85 太高改不动 → 评分保底导致好/差区分度不足 → 字面匹配导致近义表达误判（"架构设计" vs "架构升级"）→ 用户提出「以优秀简历为标杆打分」。

**最终方案**：**锚定评分**（`tool_benchmark_score`）——定义「优秀简历 = 90 分」标杆，其他简历按与标杆的差距逐项扣分：

| 标杆维度 | 标准（达标不扣） | 未达标扣分 |
| ---- | ---- | ---- |
| 技能覆盖 | JD 核心技能覆盖 ≥80% | 每少 10% 扣 ~4 分（最多 24） |
| 项目成果 | 项目维度分 ≥80（≥2 项目 或 1 项目+量化） | 最多扣 15 |
| 学历 | 满足 JD 学历要求 | 最多扣 15 |
| 业务关键词 | JD 业务词覆盖 ≥50% | 最多扣 15 |
| 超出标杆 | ≥3 项目 / 高学历+多项目 | 最多加 7（封顶 97） |

- 最低 20 分封底；**所有评估路径统一**（ReAct 成功 / fallback 兜底）
- 配套修复：
  - **英文技能大小写 Bug**（`'Java' in 'java...'` 恒 False → 统一 `s.lower()`）
  - **业务关键词语义级匹配**（4 字词核心 2 字子串命中："架构设计"→简历含"架构"即命中）
- 验证：优秀架构师 **89** / 一般开发 **59** / 差简历 **42**（极差 47 分，优秀≈90 标杆）

**相关配置**（`config.py`）：

| 配置项 | 默认值 | 含义 |
| ---- | ---- | ---- |
| `PASS_LINE` | **80** | 及格线（优秀简历可达） |
| `DIMENSION_MIN` | **15** | 单维度最低分（仅兜底展示用） |
| `TOTAL_FLOOR` | **30** | 总分下限（兜底） |
| `LLM_FALLBACK_THRESHOLD` | **35** | 保留（维度展示用，总分已统一锚定） |

`score_source` 字段统一为 `benchmark`；前端展示「ℹ️ 评分基准:以优秀简历=90 分为标杆,按技能/项目/学历/业务与标杆的差距扣分」。

### 2. 单选式 UI（目标岗位 vs JD 文本 二选一）

**原问题**：两个输入框并存，用户疑惑该用哪个、是否优先级。

**改进**（`frontend/pages/3_简历评分优化.py`）：用 `st.radio` 互斥选择

| 模式 | 输入字段 | 行为 |
| ---- | ---- | ---- |
| 🔍 **匹配目标岗位**（默认） | `target_job` 关键词 | Agent 调 RAG 自动检索 JD → 多维度匹配 |
| 📋 **用我复制的 JD 文本** | `pasted_jd` 文本 | 按粘的 JD 原文精准评估，跳过检索 |

切换模式时**自动清空对方输入**，避免用户混淆。

### 3. 候选岗位预览 + 选用（避免检索不准时 Agent 基于错误 JD 打分）

- 后端 `tool_get_job_requirements` 返回 `candidates`（最多 8 条候选岗位 + 完整 JD）
- 前端结果区底部展示候选岗位卡片（岗位名 / 公司 / 城市 / 薪资）
- 每条带「🎯 用此 JD 重新评估」按钮 → 一键替换 JD 文本重跑

### 4. 使用岗位显性告知（容错透明度）

结果区顶部展示：「📌 **本次评估使用的岗位：XXX**」+ JD 来源说明，告知用户本次评估基于哪个岗位。

### 5. 文件边界友好化（与 RAG 侧统一）

| 场景 | 原行为 | 修复后 |
| ---- | ---- | ---- |
| 纯图片 PDF | `500 + 内部路径泄露` | `400「文件中未提取到可识别的文字内容,请上传包含文字层的 PDF/DOCX」` |
| 空文件 / 损坏文件 | `500 + 路径泄露` | `400「简历文件解析失败,请检查文件是否完整...」` |
| 简历内容 <100 字 | 静默通过 | `400「简历内容过少,可能不是有效简历」` |
| 非法格式 | 500 | `400「仅支持 PDF / DOCX 格式简历文件」` |
| Ollama/RAG 异常 | 透传英文 | `503「本地大模型服务暂不可用,请确认 Ollama 已启动后重试」` / `503「岗位检索服务异常,请稍后重试,或手动粘贴目标 JD 文本后重新评估」` |

实现：`api.py` 新增 `_agent_error()` 统一错误响应（汉化 + 路径脱敏 + 400/503 分类）。

### 6. 评估入口预取岗位 JD（修复"关键词模式错误走通用评估"）

**问题**：ReAct Agent 的 LLM 不一定主动调用 `tool_get_job_requirements`，导致 `mode=通用评估`，候选列表/使用岗位全空。

**修复**（`service.py`）：评估入口（`evaluate` / `evaluate_stream`）**每次按当前输入重新预取** `session.job_requirements`（不复用旧会话缓存，因 target_job/jd_text 可能变化），保证：
- `mode` 正确（"岗位定向评估" / "通用评估"）
- `candidates` 列表有值
- `used_job_name` 仅 found 时返回岗位名

### 7. RAG 检索超时优化

`tool_get_job_requirements` 主路径 / NL2SQL 兜底超时 **60s → 12s**，RAG 挂掉时不再卡 2 分钟。

### 8. 删除 Agent 参数 slider

前端 sidebar 删除「LLM 温度 / 最大推理轮数」slider（用户反馈不必要 + 简化界面），后端仍保留配置项支持环境变量调优。

### 9. SSE 流式接口新增可选参数

`/api/agent/evaluate/stream` 新增 `temperature` / `max_iterations` form 字段（默认 None 走配置项，便于后续按需扩展）。

### 10. 优化建议增强（Agent 亮点：评分 + 优化）

**问题**：建议数量少（1-2 条）、不够具体、未突出"面向结果"。

**改进**：
- **LLM prompt**（`AGENT_SUGGESTION_SYSTEM`）：要求 **4-6 条**建议；每条必须「①明确指出简历当前不足(problem) ②给出可落地优化方向 + 含量化数字的改写示例(after)」
- 覆盖维度（按弱项优先）：①**项目经历面向结果**（"项目描述目前只写了做了什么,没有突出成果。建议 STAR 重写:背景→任务→行动→量化结果(性能提升X%/吞吐X倍/节省X人天)"）②技能关键词补充 ③业务关键词嵌入 ④**简历开头亮点**（一句话概括:年限+技术栈+最大成果）⑤面试回答准备（30 秒电梯版 + 5 分钟深挖版）⑥其他弱项
- **fallback 模板**同步强化：新增「简历开头亮点」建议，项目经历建议改名为「项目经历(面向结果)」并强调成果量化