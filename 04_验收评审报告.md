# 求职匹配 AI 系统 · 验收评审报告（RAG + Agent 双项目）

> 评审日期：2026-08-13 | 评审范围：RAG 检索链路（行业知识/岗位匹配）+ 简历评分 Agent（ReAct）
> 评审方式：代码审查 + 运行时实测（Embedding 召回 / 分块 / 异常路径）

---

## 一、文档怎么切？（Chunking 策略）

### 现状
- `CHUNK_SIZE=500` 字符 / `CHUNK_OVERLAP=50`
- 策略：按空行切段落 → 段落合并至 500 字 → 超长段按字符切割 → 块间补 50 字重叠
- 实测：252 字文档 → 1 块（248 字）；语义完整，未截断

### 评估
| 维度 | 结论 |
| ---- | ---- |
| 简历/短文档（<500字） | ✅ 单块完整，无需切分 |
| 岗位 JD（200-800字） | ✅ 1-2 块，粒度合适 |
| 长文档/行业知识（>2000字） | ⚠️ 500 字块对问答粒度偏大，且无标题感知，切分点可能落在语义中间 |

### 建议（按业务场景）
- **保持 500/50** 对当前场景（简历 + 短 JD）最优
- 若后续接入长文档：改用**标题感知分块**（按 `##/一、` 等标题为边界）+ 500 字上限，避免语义截断
- 纯字符切割对中文不友好（可能从词中间断开），建议加 `_` 边界保护（只在标点/换行处切割）

---

## 二、Embedding 怎么选？

### 现状
- `nomic-embed-text`（274MB，Ollama 官方库，免费本地）

### 实测（中文语义召回）
```
查询「Java开发工程师需要什么技能?」 → dist=0.454（相关 ✓）
查询「简历匹配度怎么样」           → dist=0.658（弱相关,库内无更相关内容）
```

### 评估
| 维度 | 结论 |
| ---- | ---- |
| 中文效果 | ⚠️ 一般。nomic-embed-text 主攻英文,中文语义区分度中等(0.45~0.73) |
| 体积/速度 | ✅ 274MB 轻量,嵌入快 |
| 一致性 | ✅ 查询与入库同一模型,无向量空间漂移 |

### 建议
- **当前够用**：知识库规模小（chunk 数 < 100），nomic 的召回精度足以支撑问答
- **规模增长后换 `bge-m3`**（Ollama 可 pull，中文优化，多语言 8192 长度）：召回精度显著提升，仅需改 `OLLAMA_EMBED_MODEL` + 重建向量库（一条命令，成本低）
- 切换注意：换模型后**必须重建向量库**（旧向量与新模型不可比）

---

## 三、召回结果准不准？

### 现状（双路召回 + 合并精排）
```
查询改写 → 双路召回 → 合并去重 → 精排取 TopK → 组装上下文 → LLM 生成
路径1: Chroma 向量语义检索（top 5, score = 1-distance）
路径2: BM25 关键词检索（top 5, jieba 分词, score 归一化）
合并: score = 0.7×语义相似度 + 0.3×召回rank加成
```

### 实测
- 库内提问「Java高级工程师需要哪些核心能力」→ 召回 3 条（PDF+DOCX）→ 回答准确结构化 ✅
- 库外提问「量子纠缠」→ 召回 2 条（无关）→ **模型明确拒绝**（防幻觉生效）✅

### 评估
| 维度 | 结论 |
| ---- | ---- |
| 双路互补 | ✅ 向量（语义）+ BM25（关键词）互补,中文分词 jieba 正确 |
| 合并权重 | ⚠️ 0.7/0.3 是启发式,无标注数据验证 |
| 无结果兜底 | ✅ 知识库为空/无匹配 → 直接问答 + 明确"未找到" |
| 岗位匹配召回 | ✅ 多源检索（本地+腾讯+Adzuna）→ 候选列表可换 JD |

### 建议
- 合并权重可配置化（`.env` 暴露 `RAG_RECALL_W=0.7`），便于调优
- 增加**召回结果人工抽检**：知识库上传后抽查 3-5 个问题,确认召回片段与问题相关

---

## 四、需不需要重排（Rerank）？

### 现状
- `OLLAMA_RERANK_MODEL` 为空 → **自动降级为向量相似度排序**（无独立 rerank 模型）

### 评估
| 场景 | 是否需要 rerank |
| ---- | ---- |
| 当前（知识库 < 100 chunk） | ❌ 不需要。召回量小,双路合并已够,加 rerank 徒增延迟 |
| 规模增长（100-1000 chunk） | ✅ 建议 `bge-reranker-v2-m3`（Ollama 可自建,对中文精排有效） |
| 岗位匹配（结构化 MySQL） | ❌ 不需要,走 NL2SQL + 规则评分 |

### 建议
- 保持现状（降级排序）即可,架构已预留 `OLLAMA_RERANK_MODEL` 配置位
- 若演示时被问到"重排"：说明「当前知识库规模小,向量+BM25 双路合并已满足精度;架构预留 rerank 接口,规模增长可无缝启用 bge-reranker」——体现架构前瞻性

---

## 五、最终怎么把检索内容交给模型？（Prompt 组装）

### 现状
```
user_prompt = "参考资料:" + context[:3000] + "\n\n问题:" + query
system_prompt = 强约束规则:
  1. 基于参考资料回答,可标注 [资料N]
  2. 资料不足 → 只回复「知识库中未找到相关资料」,禁止编造
```
- 上下文截断：RAG 问答 3000 字符 / Agent 简历 3000 / JD 2000-3000 / 观察 1500
- 引用来源：RAG 返回 sources（file_name + score），前端展示「引用来源」

### 评估
| 维度 | 结论 |
| ---- | ---- |
| 防幻觉 | ✅ system prompt 强约束 + 实测库外问题明确拒绝 |
| 上下文长度 | ✅ 3000 字符 ≈ 3 条来源 × 500 字块 + 余量,与 RAG_TOP_K=3 匹配 |
| 引用透明 | ✅ 返回 file_name/score,前端折叠展示 |

### 建议
- **保持现状**（3000 截断 + TOP_K=3 是当前场景最优配比）
- 可选增强：给每个 [资料N] 加来源文件名,答案可直接追溯（对评审展示有价值）

---

## 六、召回效果 / 上下文长度 / 回答准确率 × 业务场景调整？

### 当前业务场景画像
| 场景 | 数据规模 | 上下文需求 | 召回要求 | 结论 |
| ---- | ---- | ---- | ---- | ---- |
| 行业知识问答 | 小（<100 chunk）| 3000 字够 | 中（语义+关键词）| ✅ 当前配置最优 |
| 简历岗位匹配 | MySQL 607 条 | 规则评分,不走 LLM 长上下文 | 高（技能匹配）| ✅ 已用规则+锚定评分 |
| Agent 简历评估 | 单简历 + 单 JD | 3000+2000 够 | 高（要精准 JD）| ✅ JD 优先/候选可换 |
| 多源岗位检索 | 实时 API | 不生成长文本 | 中 | ✅ 列表展示 |

### 需要调整的点
1. **RAG_TOP_K=3 固定**：建议暴露配置,长文档场景可调 5
2. **上下文 3000 硬截断**：对极长 JD（>3000 字）会丢信息 → 已在 Agent 端用 `jd[:2000]` 保底,可接受
3. **无动态长度自适应**：小知识库可缩短上下文（省时）,大知识库可延长 → 当前统一 3000,后续可按需配置化

---

## 七、异常处理评审（6 类）

| 异常 | 现状实现 | 评审 |
| ---- | ---- | ---- |
| **模型超时** | `OLLAMA_TIMEOUT=120s` + 重试 2 次（`OLLAMA_MAX_RETRIES=2`）+ chat 生成失败降级直接问答 | ✅ 完整链路；120s 偏保守,CPU 推理 qwen2:7b 正常 5-30s/轮 |
| **Tool 调用失败** | ReAct 超轮数(6)/重复调用(2次)/未知工具 → `AgentLoopError` → **fallback 兜底编排**（规则评分+规则建议）| ✅ 保证接口始终返回结构化结果 |
| **上下文太长** | 所有 prompt 硬截断（3000/2000/1500/1200）+ TOP_K 限制 | ✅ 无 OOM 风险；截断后信息损失可控 |
| **Token 成本太高** | **全本地 Ollama,0 token 成本**（产品核心卖点）| ✅ 无需调整；只需关注**推理时延** |
| **RAG 召回不准** | 双路召回 + 防幻觉 system prompt + 候选岗位可换 JD + 引用来源可追溯 | ✅ 多层兜底；库外问题明确拒绝 |
| **用户输入异常** | pydantic 400 + 中文错误映射 + 前端表单校验（<2字/空文件/非法格式/纯图片PDF）| ✅ 全部 400 友好提示,无崩溃 |

### 评审结论：6 类异常全部有兜底,无单点故障导致服务不可用。

---

## 八、总体结论

### ✅ 通过项（符合验收要求）
1. 分块（500/50）与业务场景匹配,中文段落优先
2. 双路召回（向量+BM25）+ 防幻觉 prompt,库外问题拒绝准确
3. 重排按需启用,架构预留配置位
4. Prompt 组装强约束 + 引用来源透明
5. 6 类异常全兜底,全本地 0 token 成本
6. 锚定评分（优秀=90 标杆）可解释、区分度清晰
7. 文件/输入边界 400 友好化,无内部路径泄露

### 💡 改进建议（按优先级）
| 优先级 | 建议 | 收益 |
| ---- | ---- | ---- |
| P1 | 换 `bge-m3` Embedding（规模增长时,需重建向量库）| 中文召回精度 ↑ |
| P1 | 长文档接入标题感知分块 | 长文档问答质量 ↑ |
| P2 | 召回合并权重/TOP_K 配置化（`.env`）| 调优灵活 |
| P2 | 启用 rerank（知识库 >100 chunk 时）| 精排精度 ↑ |
| P3 | 上下文长度按知识库规模自适应 | 时延优化 |
| P3 | 答案标注来源文件名 | 可追溯性 ↑（评审展示亮点）|

### 演示话术（面试可讲）
> "我们走的是『查询改写 → 双路召回（向量语义 + BM25 关键词）→ 合并精排 → 防幻觉生成』标准 RAG 链路。当前知识库规模小,nomic-embed-text + 双路合并已满足精度;架构上预留了 bge-m3 和 rerank 的配置位,规模增长可无缝升级。异常兜底方面,模型超时走降级、Tool 失败走 fallback 编排、上下文硬截断防溢出,而且全本地推理 token 成本为 0——这是与其他云端方案的差异化亮点。"
