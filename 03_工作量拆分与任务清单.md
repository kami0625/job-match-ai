# 工作量拆分与任务清单

> 对应项目：求职匹配 AI 系统（RAG 求职匹配 + 简历评分优化 Agent）
> 编制日期：2026-08-09
> 说明：按 4 大块、17 个子项拆分。每项标注「对应代码文件 / 实现状态 / 验收标准 / 依赖关系」，状态符号：
> ✅ 已实现（代码已生成并通过语法校验）｜⚠️ 部分实现（有基础能力，需增强）｜❌ 待补充（缺口）

---

## 总览

| 块 | 名称 | 子项数 | 状态汇总 |
| ---- | ---- | ---- | ---- |
| 1 | 通用底层地基 | 7 | ✅ 全部已实现 |
| 2 | RAG 核心业务模块 | 6 | ✅ 5 项已实现；⚠️ 1 项部分实现 |
| 3 | Streamlit 前端页面 | 5 | ✅ 3 项；⚠️ 2 项部分实现 |
| 4 | 联调、报错修复、文档完善 | 9 | ✅ 6 项；⚠️ 2 项；❌ 1 项待补 |

---

## 块 1：通用底层地基

| # | 任务 | 对应文件 | 状态 | 验收标准 |
| ---- | ---- | ---- | ---- | ---- |
| 1.1 | 封装 Ollama 统一调用工具（对话 + 向量嵌入） | `app/utils/ollama_client.py` | ✅ | 单轮/多轮对话、流式、文本嵌入、rerank 精排统一入口；失败自动重试 2 次；业务代码无直连 11434 |
| 1.2 | PDF / Word 文档解析工具 | `app/utils/doc_parser.py` | ✅ | PDF（PyMuPDF）/ Word（python-docx）文本提取；按扩展名路由；段落合并 + 重叠分块 |
| 1.3 | MySQL 连接封装、岗位数据表建表 | `app/dao/mysql_db.py` | ✅ | DBUtils 连接池；`job_info` 表启动自动建表；查询/写入/插入统一封装 |
| 1.4 | Chroma 向量库工具 | `app/dao/chroma_db.py` | ✅ | PersistentClient 持久化；集合自动创建；写入/语义检索/按 doc_id 删除/计数；嵌入走本地 Ollama |
| 1.5 | 全局配置 | `app/config.py` + `.env.example` | ✅ | 全部参数从 .env 读取并集中暴露；业务代码禁止硬编码/直读环境变量 |
| 1.6 | 依赖文件 | `requirements.txt` | ✅ | 全项目依赖固定版本号；Python >= 3.10 |
| 1.7 | FastAPI 全局异常、统一返回格式封装 | `app/main.py` + `app/utils/common_tools.py` | ✅ | 统一 `{code, message, data}`；AppError / 参数校验 400 / 兜底 500 全局捕获，不透传堆栈 |

**块 1 依赖**：无。任何块开发前先完成本块。

---

## 块 2：RAG 核心业务模块

| # | 任务 | 对应文件 | 状态 | 验收标准 |
| ---- | ---- | ---- | ---- | ---- |
| 2.1 | 文档批量上传、增量入库接口 | `app/modules/rag_module/api.py` + `service.py` | ⚠️ | 单文件上传 ✅ 已实现；**批量多文件上传 ❌ 待补**；入库采用 doc_id 维度增量（新增不重建全库）✅ |
| 2.2 | BM25 + 向量混合召回实现 | `service.py`（`BM25Index` + `_recall`） | ✅ | jieba 分词 + rank_bm25 关键词召回 + 向量语义召回双路合并去重；BM25 索引按文档数自动重建 |
| 2.3 | bge-reranker 本地重排逻辑 | `ollama_client.rerank` + `service.chat` | ✅ | 调用本地 rerank 模型精排取 TopK；模型缺失时自动降级余弦相似度排序 |
| 2.4 | 查询改写优化逻辑 | `service._rewrite_query` | ✅ | 大模型对模糊提问扩写补全，仅输出改写结果；失败回退原查询 |
| 2.5 | NL2SQL 自然语言转 MySQL 工具 + 接口 | `service.query_jobs/_generate_sql/_clean_sql` + `api /query` | ✅ | 大模型生成 SQL；白名单（仅 SELECT）+ 黑名单（写操作/多语句）双校验；自动补 LIMIT；执行结果 + 生成 SQL 返回 |
| 2.6 | 简历 - JD 匹配度计算逻辑 | `service.match_resume/_llm_match_score/_rule_match_score` + `generate_match_chart` | ✅ | 大模型四维评分（技能/经验/学历/项目契合）；解析失败规则兜底；Matplotlib 柱状图 base64 返回 |

**块 2 依赖**：块 1（utils/dao/config）。

---

## 块 3：Streamlit 前端页面

| # | 任务 | 对应文件 | 状态 | 验收标准 |
| ---- | ---- | ---- | ---- | ---- |
| 3.1 | 文件拖拽上传 | 板块 A / 板块 B 的 `st.file_uploader` | ✅ | Streamlit 原生支持拖拽上传 PDF/DOCX；上传后调后端解析/入库 |
| 3.2 | 对话流式输出 | `pages/1_岗位智能匹配.py` Tab3 | ⚠️ | 后端 `ollama_client` 已具备流式能力，但 `/api/rag/chat` 当前为非流式接口，前端等待后一次性渲染。**流式接口 + 前端打字机效果 ❌ 待补** |
| 3.3 | 岗位表格展示 | 板块 A Tab2 `st.dataframe` | ✅ | NL2SQL 结果表格 + 岗位库全量列表分页展示 |
| 3.4 | 匹配度可视化图表 | 板块 A Tab4 / 板块 B | ✅ | 前端 `st.bar_chart` + 后端 Matplotlib 图（base64）双通道展示 |
| 3.5 | 多页面导航、接口请求封装 | `frontend/Home.py` + `pages/` | ⚠️ | Streamlit 多页面侧边栏导航 ✅；后端地址侧边栏配置 + session_state 共享 ✅；**请求封装散落两个页面，建议抽公共 `frontend/api_client.py`** |

**块 3 依赖**：块 1、块 2（前端只调接口，不写业务逻辑）。

---

## 块 4：联调、报错修复、文档完善

| # | 任务 | 对应文件 | 状态 | 验收标准 / 处理方案 |
| ---- | ---- | ---- | ---- | ---- |
| 4.1 | Ollama 超时处理 | `config.py`（OLLAMA_TIMEOUT）+ `ollama_client` | ✅ | 请求超时可控；超时/未启动时重试 2 次后返回明确错误，不崩溃 |
| 4.2 | 内存溢出处理 | 各 prompt 截断（简历 [:4000]、JD [:3000]）| ⚠️ | 长文截断 ✅；大文档分块限长 ✅；**可再补充：上传文件大小上限校验（如 20MB）与嵌入批量分片** |
| 4.3 | 大模型幻觉 | `service.chat` system prompt | ✅ | 提示词约束「仅依据参考资料回答，找不到明确说明不编造」；回答附引用来源 |
| 4.4 | SQL 语法错误拦截 | `service._clean_sql` | ✅ | 非 SELECT / 写操作关键字 / 多语句注入均拦截，返回 400 |
| 4.5 | 向量库重复入库 | `service.upload_document` | ❌ | **待补：同一文件重复上传时按 doc_id 先删旧块再写入（覆盖更新），避免向量库膨胀** |
| 4.6 | 分块优化调参 | `.env`（CHUNK_SIZE / CHUNK_OVERLAP / TOP_K）| ✅ | 分块大小、重叠、召回数均可配置调参，无需改代码 |
| 4.7 | 项目 README | `README.md` | ✅ | 架构图、目录结构、环境准备、启动步骤、接口一览、规范说明 |
| 4.8 | 接口文档 | FastAPI `/docs` + README 接口表 | ✅ | Swagger 自动生成；markdown 版接口清单已入 README |
| 4.9 | 演示素材 | `data/`（待补充）| ❌ | **待补：示例岗位数据 SQL（10 条左右）+ 示例岗位 JD 文档 + 示例简历，供演示与测试** |

**块 4 依赖**：块 1/2/3 联调后进行。

---

## 缺口汇总（❌ / ⚠️ 中待办）

| 优先级 | 缺口 | 类型 | 建议实现方式 |
| ---- | ---- | ---- | ---- |
| P1 | 向量库重复入库去重 | 功能修复 | `upload_document` 增加 doc_id 级覆盖：先 `delete_by_doc_id(同名文档)` 或按文件内容哈希去重，再写入 |
| P1 | 对话流式输出 | 功能增强 | 新增 `POST /api/rag/chat/stream`（SSE）；前端用 `st.write_stream` 或 requests 流式渲染 |
| P2 | 批量文档上传 | 功能增强 | `POST /api/rag/upload` 支持 `List[UploadFile]`，循环入库并汇总结果 |
| P2 | 前端请求封装 | 代码优化 | 抽 `frontend/api_client.py`：统一 base_url、超时、错误提示 |
| P3 | 演示素材 | 资料准备 | 生成 `data/demo/` 示例岗位 SQL、示例 JD/简历（PDF/DOCX），附导入说明 |
| P3 | 上传大小限制 / 嵌入分批 | 健壮性 | 接口层校验文件大小；嵌入 >100 条时分批调用 |

---

## 建议的开发与认领顺序

```
第 1 步 → 块 1（7 项，无依赖）           [可 1 人独立完成]
第 2 步 → 块 2（依赖块 1）               [与块 3 可并行]
第 3 步 → 块 3（依赖块 1/2 接口契约）    [与块 2 可并行]
第 4 步 → 块 4（联调 + 缺口修复 + 文档） [依赖块 2/3]
```

并行建议：块 2 与块 3 可分别由 2 人并行开发（以接口契约为边界）；块 1 是地基必须先行；块 4 由一人统筹联调收尾。
